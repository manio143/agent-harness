# Archived notes

This directory contains older analysis / progress summaries that were useful during active development but are not meant to be the primary docs entrypoint.

They are kept for historical context (“why we did X”), but may be out of date relative to current behavior.
