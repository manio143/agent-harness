<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Agent.Acp.Client.AvaloniaApp.ViewModels.Connection"
             xmlns:c="clr-namespace:Agent.Acp.Client.AvaloniaApp.Converters"
             mc:Ignorable="d"
             x:Class="Agent.Acp.Client.AvaloniaApp.Views.Connection.ConnectionView"
             x:DataType="vm:ConnectionViewModel">

  <Border Padding="24" Background="#111318">
    <StackPanel Spacing="14" MaxWidth="760">
      <TextBlock Text="Connect to ACP agent (stdio)" FontSize="22" FontWeight="SemiBold" Foreground="#E6E6E6" />
      <TextBlock Text="This client currently launches an ACP agent process and talks over line-delimited JSON-RPC on stdio." Opacity="0.8" Foreground="#B9C0CC" TextWrapping="Wrap"/>

      <StackPanel Spacing="6">
        <TextBlock Text="Command" Foreground="#E6E6E6" />
        <TextBox Text="{Binding Command}" PlaceholderText="dotnet" Background="#1A1D24" Foreground="#E6E6E6" />
      </StackPanel>

      <StackPanel Spacing="6">
        <TextBlock Text="Arguments" Foreground="#E6E6E6" />
        <TextBox Text="{Binding Arguments}" PlaceholderText="run --project src/Agent.Server" Background="#1A1D24" Foreground="#E6E6E6" />
      </StackPanel>

      <StackPanel Spacing="6">
        <TextBlock Text="Working directory" Foreground="#E6E6E6" />
        <TextBox Text="{Binding WorkingDirectory}" Background="#1A1D24" Foreground="#E6E6E6" />
      </StackPanel>

      <Border Background="#2A1515" CornerRadius="6" Padding="10" IsVisible="{Binding Error, Converter={x:Static c:StringConverters.IsNotNullOrEmpty}}">
        <TextBlock Text="{Binding Error}" Foreground="#FFB4B4" />
      </Border>

      <StackPanel Orientation="Horizontal" Spacing="10">
        <Button Content="Connect" Command="{Binding ConnectCommand}" Background="#3B82F6" Foreground="#FFFFFF" />
        <Button Content="Settings" IsEnabled="False" />
      </StackPanel>

      <TextBlock Text="Next: session/list → session/new → session/prompt" Opacity="0.8" Foreground="#B9C0CC" />
    </StackPanel>
  </Border>
</UserControl>
